version: "3.8"

services:
  app:
    container_name: supplier-portal-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:3306/db-supplier-portal
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: Lui$05012013
    networks:
      - supplier-network
    restart: unless-stopped

networks:
  supplier-network:
    driver: bridge
